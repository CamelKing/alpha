/**
 * return the exact value of the (first) argument being passed in
 *
 * @since 0.0.1
 * @category Object
 *
 * @export
 * @param {*} value
 * @returns {*}
 */

export function _identity(value: any): any {
  return value;
}
